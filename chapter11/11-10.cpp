#include <iostream>
#include <vector>
#include <list>
#include <map>

using namespace std;

int main()
{
    //不行，无法定义
    /*
    map<list<int>::iterator, int> > lstimap;
    map<vector<int>::iterator, int> > iterimap;
    */
    return 0;
}