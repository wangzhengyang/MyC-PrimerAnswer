运算符优先级问题，导致该表达式无法通过编译，问题在哪？如何修改？

string s = "word";
string p1 = s + s[s.size() - 1] == 's'? "":"s";
由于+运算符优先级最高，所以优先执行s + s[s.size() - 1];
其次优先级是==运算符，来判断前面是否等于's'
最后是执行条件运算符

修改如下:
string p1 = s + (s[s.size() - 1] == 's'? "":"s");